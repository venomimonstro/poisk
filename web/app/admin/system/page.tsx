"use client";

import { useEffect,useState } from "react";

type AdminMe={admin_id:number;email:string;role:string;expires_at:string};
type Diagnostics={
 crawler:{domains:number;urls:number;ready:number;leased:number;retry:number;dead:number;success_24h:number;retry_24h:number;dead_24h:number;blocked_24h:number};
 index:{ready:number;leased:number;retry:number;dead:number;processed_24h:number;indexed_urls:number;excluded_urls:number;error_urls:number;average_quality:number;average_spam:number};
 demand:{watch:number;open:number;resolved:number;suppressed:number;active_feedback:number};
 webmaster:{users:number;sites:number;pending_verifications:number;queued_sitemaps:number;queued_url_requests:number};
 billing:{accounts:number;active_subscriptions:number;past_due_subscriptions:number;open_invoices:number;failed_invoices:number;unprocessed_payment_events:number};
 data_hub:{draft:number;published:number;suppressed:number;publication_events_24h:number;trends_today:number};
 capacity?:{snapshot_id:number;measured_documents:number;measured_at:string};
};
const card:React.CSSProperties={border:"1px solid #e2e2e2",borderRadius:12,padding:16,background:"#fff"};
async function read<T>(url:string):Promise<T>{const r=await fetch(url,{cache:"no-store",credentials:"same-origin"});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d?.error||`http_${r.status}`);return d as T}
export default function SystemPage(){const[me,setMe]=useState<AdminMe|null>(null);const[d,setD]=useState<Diagnostics|null>(null);const[error,setError]=useState("");async function load(){try{const[m,diag]=await Promise.all([read<AdminMe>("/api/admin/me"),read<Diagnostics>("/api/admin/diagnostics")]);setMe(m);setD(diag);setError("")}catch(e){setError(e instanceof Error?e.message:"unavailable")}}useEffect(()=>{void load()},[]);if(error)return <main style={{maxWidth:1100,margin:"40px auto",padding:24}}><a href="/admin">← Admin</a><h1>System diagnostics</h1><p>{error}</p></main>;if(!me||!d)return <main style={{maxWidth:1100,margin:"40px auto",padding:24}}>Загрузка…</main>;return <main style={{maxWidth:1180,margin:"32px auto",padding:24,display:"grid",gap:16}}><header style={{display:"flex",justifyContent:"space-between",alignItems:"center"}}><div><a href="/admin">← Admin</a><h1 style={{margin:"8px 0 0"}}>System diagnostics</h1><small>{me.email} · {me.role}</small></div><button onClick={()=>void load()}>Обновить</button></header><div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(250px,1fr))",gap:12}}><section style={card}><strong>Web index</strong><div>Indexed {d.index.indexed_urls.toLocaleString("ru-RU")}</div><div>Excluded {d.index.excluded_urls} · error {d.index.error_urls}</div><div>Quality {d.index.average_quality.toFixed(1)} · spam {d.index.average_spam.toFixed(1)}</div><div>Outbox ready {d.index.ready} · retry {d.index.retry} · dead {d.index.dead}</div></section><section style={card}><strong>Crawler</strong><div>{d.crawler.domains} domains · {d.crawler.urls} URLs</div><div>24h success {d.crawler.success_24h}</div><div>retry {d.crawler.retry_24h} · dead {d.crawler.dead_24h} · blocked {d.crawler.blocked_24h}</div></section><section style={card}><strong>Demand</strong><div>open {d.demand.open} · watch {d.demand.watch}</div><div>resolved {d.demand.resolved} · suppressed {d.demand.suppressed}</div><div>active feedback {d.demand.active_feedback}</div></section><section style={card}><strong>Data Hub</strong><div>published {d.data_hub.published} · draft {d.data_hub.draft}</div><div>suppressed {d.data_hub.suppressed}</div><div>events 24h {d.data_hub.publication_events_24h} · trends today {d.data_hub.trends_today}</div></section><section style={card}><strong>Webmaster</strong><div>{d.webmaster.users} users · {d.webmaster.sites} sites</div><div>pending verification {d.webmaster.pending_verifications}</div><div>sitemaps {d.webmaster.queued_sitemaps} · URL ops {d.webmaster.queued_url_requests}</div></section><section style={card}><strong>Billing</strong><div>{d.billing.active_subscriptions} active subscriptions</div><div>past due {d.billing.past_due_subscriptions}</div><div>open invoices {d.billing.open_invoices} · failed {d.billing.failed_invoices}</div><div>unprocessed events {d.billing.unprocessed_payment_events}</div></section></div><section style={card}><strong>Capacity</strong>{d.capacity?<><div>Snapshot #{d.capacity.snapshot_id}</div><div>Measured documents {d.capacity.measured_documents.toLocaleString("ru-RU")}</div><div>{new Date(d.capacity.measured_at).toLocaleString()}</div></>:<div>Нет измеренного Capacity snapshot — коммерческий gate не закрыт.</div>}</section><section style={{...card,borderColor:"#f0c36d"}}><strong>Observability gaps</strong><p style={{margin:"8px 0 0"}}>Search Quality и Answer Engine пока не имеют постоянной истории runtime-запусков в PostgreSQL. Admin не подменяет их состояние косвенными метриками; persistent history будет добавлена отдельным блоком Sprint 23.</p></section></main>}
